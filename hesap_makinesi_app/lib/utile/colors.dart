import 'package:flutter/material.dart';

class AppColor {
  // Kutu rengi
  static const Color boxcolors = Color(0xFFA3E764);

  // Ana arka plan rengi
  static const Color BackgroundColor = Color(0xFF20272A);

  // Kenar rengi
  static const Color BorderColor = Color.fromARGB(255, 0, 112, 80);

  // Buton rengi
  static const Color ButtomColor = Color.fromRGBO(40, 46, 52, 1);

  // Metin rengi
  static const Color TextColor = Color(0xFF005B41);

  // Açık metin rengi
  static const Color TextColorlight = Color(0xFF13FFBC);
}
