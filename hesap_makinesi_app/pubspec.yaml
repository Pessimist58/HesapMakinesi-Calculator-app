name: hesap_makinesi_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  cupertino_icons: ^1.0.6
  firebase_core: ^3.3.0
  firebase_core_web: ^2.17.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  fonts:
    - family: digital
      fonts:
        - asset: asset/digital.ttf 

assets:
 -  asset/logo.png # not çalışmıyor...
